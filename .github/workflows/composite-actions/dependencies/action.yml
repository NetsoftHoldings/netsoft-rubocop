name: Install dependecies

runs:
  using: composite
  steps:
    - name: Install Ruby
      uses: ruby/setup-ruby@v1
      with:
        ruby-version: 3.2
        bundler-cache: true
        cache-version: 1.1
